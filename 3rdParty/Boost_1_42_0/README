This lightweight version of Boost was created with the Boost bcp utility.

Command that was used to create the current subset:
    mkdir ./output
    bcp bind function lexical_cast noncopyable optional program_options smart_ptr signals signals2 thread ./output
